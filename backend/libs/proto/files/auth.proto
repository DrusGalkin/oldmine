syntax = "proto3";

option go_package= "../generate";

service Auth {
  rpc CheckAuth (AuthRequest) returns (AuthResponse);
}

message AuthRequest {
  string sess_id = 1;
}

message AuthResponse {
  int64 id = 1;
  string name = 2;
  string email = 3;
  bool auth = 4;
}